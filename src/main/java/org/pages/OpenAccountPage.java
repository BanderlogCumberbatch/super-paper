package org.pages;

import org.openqa.selenium.WebDriver;
import org.pages.elements.BankManagerMenuElements;

public class OpenAccountPage extends BankManagerPage {

    public OpenAccountPage(final WebDriver webDriver) { super(webDriver); }
}
